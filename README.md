# UpTraderDjango - Иерархическая система меню

Django-приложение для создания и отображения иерархических древовидных меню с поддержкой навигации и визуальной иерархии.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install django
```

### Миграции

```bash
python manage.py makemigrations
python manage.py migrate
```


### Запуск сервера разработки

```bash
python manage.py runserver 8080
```

Сервер будет доступен по адресу: `http://127.0.0.1:8080/`
Основа сервиса бужт распологаться по `http://127.0.0.1:8080/menu_tree/`, это сделано, чтобы
небыло проблем с доступом к панели admin

## 📋 Описание проекта



Это Django-приложение реализует систему иерархических меню с следующими возможностями:

- **Иерархическая структура**: Меню организованы в древовидную структуру с родительскими и дочерними элементами
- **Динамическая навигация**: Автоматическое определение текущего положения в меню
- **Визуальная иерархия**: Различные стили для родителей, текущего элемента и дочерних элементов
- **Админ-интерфейс**: Удобное управление меню через Django Admin

## 🏗️ Архитектура

### Модель данных

```python
class Menu(models.Model):
    name = models.CharField(max_length=200, unique=True)  # Уникальное имя меню
    parent = models.ForeignKey('self', ...)              # Родительский элемент
    url = models.CharField(max_length=255, default='')   # URL для перехода
```

## 🎯 Использование

### Создание меню через админку

1. Перейдите в админку: `http://127.0.0.1:8080/admin/`
2. Создайте элементы меню, указывая родительские связи
3. Для корневых элементов оставьте поле `parent` пустым

### URL-структура

- `/menu_tree/` - Отображение всех корневых меню
- `/menu_tree/<имя_элемента>/` - Отображение меню с выделенным элементом

